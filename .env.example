# --- Telegram ---
BOT_TOKEN=123456:AA...              # @BotFather
OWNER_CHAT_ID=                      # куда падают алерты о поломке синхронизации
TELEGRAM_PROXY=                     # socks5://user:pass@host:1080 — если api.telegram.org недоступен напрямую

# --- Шифрование токенов доступа (пароли не хранятся) ---
# openssl rand -base64 32
CREDENTIALS_KEY=

# --- База ---
DATABASE_URL=postgresql+asyncpg://bot:bot@db:5432/bot

# --- Публичный ICS-фид ---
PUBLIC_BASE_URL=https://schedule.example.ru

# --- Источник ---
RANEPA_BASE_URL=https://my.ranepa.ru
SYNC_HOUR_MSK=3
SYNC_CONCURRENCY=4
